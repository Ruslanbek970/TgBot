# --- Converter (Developer 2) ---
pdf2docx>=0.5.8        # PDF -> Word
PyMuPDF<1.24.0         # Fix for 'Rect' object has no attribute 'get_area' in pdf2docx
google-generativeai>=0.4.0
img2pdf>=0.5.0         # images -> PDF (lossless)
# LibreOffice is installed at SYSTEM level, not via pip (see README): office -> PDF

# --- Bot (Developer 3, minimal version) ---
pyTelegramBotAPI>=4.14.0
python-dotenv>=1.0.0

# --- Storage (Developer 1) ---
psycopg2-binary>=2.9.9

# --- Fuzzy Search (Developer 3) ---
rapidfuzz>=3.0.0
